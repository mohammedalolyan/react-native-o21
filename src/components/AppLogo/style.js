import { StyleSheet } from "react-native";
import { Colors } from "@common";

export default StyleSheet.create({
  image: {
    height: 20,
    tintColor: "black",
    resizeMode: "contain",
    alignItems: "center"
  }
});
